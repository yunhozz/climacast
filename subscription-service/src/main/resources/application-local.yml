server:
  port: 8090

spring:
  application:
    name: subscription-service
  cloud:
    config:
      enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka